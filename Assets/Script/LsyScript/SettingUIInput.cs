using UnityEngine;

public class SettingUIInput : MonoBehaviour, IInputable
{
    public void ExecuteInput()
    {
        /*[SerializeField] private TopbarUI topbar;

        //키 입력 및 처리부
        if (Input.GetKeyDown(KeyCode.Escape))
        {
            if (GameIsPaused)
            {
                topbar.menuCanvas.SetActive(false);
                topbar.Time.timeScale = 1f;
                topbar.GameIsPaused = false;
            }
            else
            {
                topbar.menuCanvas.SetActive(true);
                topbar.Time.timeScale = 0f;
                topbar.GameIsPaused = true;
            }
        }

        if (Input.GetKeyDown(KeyCode.Space))
        {
            topbar.GaugeImage(0.1f);
        }*/
    }
}
